# private-cloud-autoscaling-platform
> Mini cloud privé en 2 approches : Proxmox VE (cluster distribué via ZeroTier + HAProxy Round Robin) et VMware local (HAProxy Least Connections + Prometheus/Alertmanager + autoscaling automatique). CPU > 80% → Flask déployé sur web3 sans intervention humaine.
